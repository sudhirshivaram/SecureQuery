[project]
name = "secure-query"
version = "0.1.0"
description = "AI-Powered Security Log Analyzer using RAG"
requires-python = ">=3.9"

[tool.uv]
dev-dependencies = [
    "pytest>=7.0.0",
    "black>=23.0.0",
    "flake8>=6.0.0",
]
